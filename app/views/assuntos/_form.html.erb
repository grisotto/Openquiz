<%= simple_form_for(@assunto) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nome_assunto %>
    <%= f.input :descricao %>
    <%= f.input :link_image %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
